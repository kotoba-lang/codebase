{:schema "kotoba.repository-role-rules.v0"
 :this-repo
 {:name "codebase"
  :tier :t1
  :role :library
  :must-not [:compile :execute-components :grant-runtime-authority]
  :depends-on ["kotoba-lang/dag-cbor"
               "kotoba-lang/io-ipld"
               "kotoba-lang/io-multiformats"
               ;; Each of the three below is required by a namespace under
               ;; `src`, so each is part of the shipped closure this list is
               ;; supposed to describe:
               ;;   kotoba-kir       kotoba.codebase.typed-eval  (kotoba.kir)
               ;;   kotobase-storage kotoba.codebase.backend.kotobase
               ;;   org-ietf-ed25519 kotoba.codebase.publication (ed25519.core)
               ;; They were added to deps.edn as the IR, storage and
               ;; publication seams landed; this declaration never followed.
               "kotoba-lang/kotoba-kir"
               "kotoba-lang/kotobase-storage"
               "kotoba-lang/org-ietf-ed25519"]}}
